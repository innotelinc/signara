# ==========================================================================
# Secrets — EXAMPLE ONLY
#
# NEVER commit real secrets. Build the live Secret from your .env before
# applying, e.g.:
#
#   kubectl create secret generic signara-secrets \
#     --namespace signara \
#     --from-env-file=.env
#
# or use an external secret operator (External Secrets / SOPS) — see
# docs/Security.md § Secrets management.
# ==========================================================================
apiVersion: v1
kind: Secret
metadata:
  name: signara-secrets
  namespace: signara
  labels:
    app.kubernetes.io/part-of: signara
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  S3_ACCESS_KEY: "REPLACE_ME"
  S3_SECRET_KEY: "REPLACE_ME"
  MEILISEARCH_API_KEY: "REPLACE_ME"
  CRYPTO_MASTER_KEY: "REPLACE_ME"
  JWT_ACCESS_SECRET: "REPLACE_ME"
  JWT_REFRESH_SECRET: "REPLACE_ME"
  OIDC_CLIENT_ID: "REPLACE_ME"
  OIDC_CLIENT_SECRET: "REPLACE_ME"
  AUTHENTIK_SECRET_KEY: "REPLACE_ME"
  AUTHENTIK_DB_PASSWORD: "REPLACE_ME"
  STRIPE_SECRET_KEY: ""
  STRIPE_WEBHOOK_SECRET: ""